# iOS 快捷指令完整配置步骤

## 第 1 步：创建快捷指令

1. 打开 **快捷指令** App（绿色图标）
2. 底部确保在 **"所有快捷指令"** 标签
3. 点击右上角 **+**
4. 点击顶部中间的 **"新建快捷指令"** → 输入 **"保存推文"** → 点击键盘上的 **完成**

## 第 2 步：添加操作

5. 点击底部搜索框，输入：**URL内容**
6. 选择 **"获取URL内容"**

## 第 3 步：配置操作

7. 点击刚添加的 **"获取URL内容"** 操作，展开配置：

### 设置 URL
- 找到 **URL** 字段（在"方法"上方）
- 输入：`http://124.220.236.129:3000/api/archive`

### 设置方法
- **方法**：选择 **POST**

### 设置请求体
- **请求体**：点击展开 → 选择类型 **JSON**
- 点击 **"添加新字段"** → 选择 **"文本"**
- **键**：输入 `url`
- **值**：点击右侧的 **变量按钮**（蓝色 magic wand 图标或显示"文本"的地方）

## 第 4 步：选择变量（最关键的一步）

8. 点击值字段后，底部会弹出选项栏：
   - **选择变量** | 当前日期 | 剪贴板

9. 点击 **"选择变量"**

10. 在弹出的变量列表中，找到并选择：
    - **快捷指令输入** → **URL**

    （如果列表里没有"快捷指令输入"，先返回，见下面的备选方案）

## 第 5 步：配置分享扩展

11. 点击左上角返回箭头，回到主界面
12. 点击顶部 **"保存推文"** 名称右边的 **下拉箭头 ⌄**
13. 在弹出菜单中，如果看到 **"在共享表单中显示"**，打开开关
14. 如果没有这个选项，继续往下看"备选方案"

## 第 6 步：保存

15. 点击右上角 **"完成"**

---

## 备选方案：如果找不到"快捷指令输入"变量

如果在第 10 步找不到"快捷指令输入"，按以下方式配置：

1. 在快捷指令主界面（显示"获取URL内容"操作的页面）
2. 点击顶部名称右边的 **下拉箭头 ⌄**
3. 选择 **"重新命名"** 确认名字是 **"保存推文"**
4. 关闭菜单
5. 在"获取URL内容"操作中，url 字段的值直接输入：
   ```
   快捷指令输入的URL
   ```
   或者点击变量按钮后，在搜索框输入"输入"，找到"快捷指令输入"

---

## 备选方案：如果没有"在共享表单中显示"选项

新版 iOS 可能在快捷指令详情菜单里找不到这个开关。替代方法：

1. 完成快捷指令配置后保存
2. 打开 **设置** → **辅助功能** → **触控** → **轻点背面**
3. 选择 **"轻点两下"** 或 **"轻点三下"**
4. 在列表底部找到并选择 **"保存推文"** 快捷指令
5. 这样：在 Twitter App 中点击分享 → 复制链接 → 轻点背面 → 自动保存

---

## 使用方法

### 方式一：分享菜单（如果配置了共享表单）
1. 在 Twitter App 中看到好推文
2. 点击推文下方的 **分享按钮**（向上箭头）
3. 在底部分享菜单中找到 **"保存推文"**
4. 点击，等待几秒，完成

### 方式二：复制链接 + 轻点背面（如果配置了背面触发）
1. 在 Twitter App 中点击分享 → **复制链接**
2. 轻点 iPhone 背面两下或三下
3. 自动保存

---

## 查看保存的推文

浏览器访问：`http://124.220.236.129:3000`
